### Uthman

1. Given the list below:

   ```python
   names = ["Uthman", "Emmanuel", "Muhammed", "Destiny", "Ayomide", "Maleek", 90, True, 192.12]
   ```

   Write a statement to access the last character of `"Maleek"`.

2. Using the same list, write the code to:

   * Remove `90`
   * Remove the last two items
   * Add `"AbdulHakeem"` to the end of the list
   * Display the updated list

---

### Ayomide

1. Given:

   ```python
   nums = [13, 12, 92, 13, "Love", "Demon"]
   words = [names, nums]
   ```

   Write a statement to access the letter **'m'** in `"Demon"`.

2. Write the code to sort the following list in ascending and descending order:

   ```python
   values = [34, 6, 89, 12, 45, 2]
   ```

---

### Destiny

1. Given:

   ```python
   words = [names, nums]
   ```

   Write a statement to access the value `90` from the nested list.

2. Write the code to add `"Joseph"` and `"Anne"` to the end of the `names` list using a single method.

---

### Emmanuel

1. Given:

   ```python
   names = ["Uthman", "Emmanuel", "Muhammed", "Destiny", "Ayomide", "Maleek", 90, True, 192.12]
   ```

   Write a statement to extract the substring `"tin"` from `"Destiny"`.

2. Write the code to insert `"Gideon"` as the first element of the `names` list.

---

### Malek

1. Given:

   ```python
   nums = [-1, 19, 25, True, 94, 0, False]
   ```

   Write the code to sort the list and store the result in a variable named `result`.

2. Write the code to remove the last element from the `nums` list and display the updated list.

---

### Muhammed

1. Given:

   ```python
   names = ["Uthman", "Emmanuel", "Muhammed", "Destiny", "Ayomide", "Maleek", 90, True, 192.12]
   nums = [13, 12, 92, 13, "Love", "Demon"]
   words = [names, nums]
   ```

   Write a statement to access the string `"Love"` using the `words` list.

2. Write the code to sort the `names` list in reverse alphabetical order after removing all non-string values.
